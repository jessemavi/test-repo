name: test

on:
  workflow_dispatch:
 
        
jobs:
  echo_runner_job:
    environment: TEST
    runs-on: self-hosted
    steps:
      - name: "Echo Runner"
        run: echo "Runner - ${{ secrets.TEST47KB1 }}"
      - name: "Echo Runner"
        run: echo "Runner - ${{ secrets.TEST47KB2 }}"
      - name: "Echo Runner"
        run: echo "Runner - ${{ secrets.TEST47KB3 }}"
      - name: "Echo Runner"
        run: echo "Runner - ${{ secrets.TEST47KB4 }}"
      - name: "Echo Runner"
        run: echo "Runner - ${{ secrets.TEST47KB5 }}"
      - name: "Echo Runner"
        run: echo "Runner - ${{ secrets.TEST47KB6 }}"
      - name: "Echo Runner"
        run: echo "Runner - ${{ secrets.TEST47KB7 }}"
